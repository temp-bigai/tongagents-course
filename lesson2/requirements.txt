# Lesson 2 依赖清单 (本地 wheel 方式)
#
# tongagents + tongagents_cli 不在这里列, 通过 pip install ../wheels/*.whl 装
# (见 INSTALL.md 第 2 节)
#
# 完整安装文档：见 INSTALL.md

# === 测试依赖 ===
pytest>=7.4.0
pytest-asyncio>=0.23.0

# === 可选：日志 / 类型提示 ===
python-dotenv>=1.0.0
