# ============================================================
# Lesson 4 环境变量模板 —— 复制为 .env 并填入真实值
#   cp .env.example .env
# .env 已被 .gitignore 忽略，不会被提交到仓库
# ============================================================

# --- 必填：OpenAI API Key ---
# 官方 Key 申请：https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key

# --- 可选：API 端点（默认官方地址；使用 OpenAI 兼容服务时修改） ---
OPENAI_BASE_URL=https://api.openai.com/v1

# --- 可选：模型名（默认 gpt-5.6-luna：当前最便宜的工具调用模型） ---
OPENAI_MODEL=gpt-5.6-luna

# ---------- 方案 B：使用智谱 GLM（OpenAI 兼容接口）----------
# 注意两个端点不通用：
#   按 API 用量计费（账户余额）: https://open.bigmodel.cn/api/paas/v4
#   GLM Coding Plan（套餐额度）: https://open.bigmodel.cn/api/coding/paas/v4
# 用 Coding Plan 的 Key 必须配 coding 端点，否则报 1113 余额不足。
# OPENAI_API_KEY=你的智谱 API Key（https://open.bigmodel.cn/usercenter/apikeys）
# OPENAI_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4
# OPENAI_MODEL=glm-5.3
